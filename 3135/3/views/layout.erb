<html>
  <head>
    <title>MUNCH BUNCH</title>
  </head>

  <body> 
    <div>
      <% if current_user %>
        <%= current_user.username %>
        <a href="/logout">log out</a>
      <% else %>
        <a href="/login">login</a>
        <a href="/registration">register</a>
      <% end %>
      <hr>
    </div>

    <div align="center">
      <h1><a href="/" style="text-decoration: none">MUNCH BUNCH</a></h1>
    </div>

    <div>
      <%= yield %>

      <% flash.keys.each do |type| %>
        <% case type %>
        <% when :user_err %>
          <%= flash[type].full_messages.join('; ') %>
        <% when :review_err %>
          <%= flash[type].full_messages.join('; ') %>
        <% when :text_err %>   
          <%= flash[type] %>
        <% end %>
      <% end %>
    </div>

    <div align="right">
      <hr>
      rubizza MMXIX
    </div>
  </body>
</html>
